// Placeholder for when Firebase access works.

export async function syncCartToFirebase(_items) {
  // TODO: after invite, implement:
  // import { db } from "@/lib/firebase";
  // setDoc(doc(db, "carts", userId), { items: _items, updatedAt: serverTimestamp() })
  return true;
}
